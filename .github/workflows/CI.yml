name: CI

on:
  push:
    paths-ignore:
      - '*.org'
      - '*.md'
  pull_request:
    paths-ignore:
      - '*.org'
      - '*.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        emacs: [28.1, 29.1, 30.1, 31.1, snapshot]
        auctex: [yes, no]
    continue-on-error: ${{ matrix.emacs == 'snapshot' }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: purcell/setup-emacs@bdc64dc730ae1fcba200bfd52cb1b4cf6159cbe5 # v8.0
        with:
          version: ${{ matrix.emacs }}

      - name: Install dependencies
        run: |
          emacs -Q --batch \
            --eval "(package-initialize)" \
            --eval "(package-refresh-contents)" \
            --eval "(unless (package-installed-p 'compat) (package-install 'compat))"
          if [ "${{ matrix.auctex }}" = yes ]; then
            emacs -Q --batch \
              --eval "(package-initialize)" \
              --eval "(unless (package-installed-p 'auctex) (package-install 'auctex))"
          fi

      - name: Compile
        run: |
          emacs -Q --batch -L . \
            --eval "(setq byte-compile-error-on-warn t)" \
            --eval "(package-initialize)" \
            -f batch-byte-compile \
            *.el

      - name: Run tests
        env:
          WITH_AUCTEX: ${{ matrix.auctex }}
        run: ./run-tests.sh
